<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Aktifkan warna untuk console -->
    <conversionRule conversionWord="clr"
                    converterClass="org.springframework.boot.logging.logback.ColorConverter"/>

    <!-- === Console Appender === -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint}
                [%clr(%-5level)]
                [%clr(%X{traceId:-N/A}){cyan}]
                [%clr(%X{method:-N/A}) %clr(%X{path:-N/A}){green}]
                %clr(%logger{36}){blue} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- === File Appender (dengan rolling harian) === -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/app-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory> <!-- Simpan log 14 hari -->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId:-N/A}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
